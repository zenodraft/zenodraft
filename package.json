{
  "name": "zenodo-publish",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm `find build/ -type f -name *.js` 2> /dev/null && rm `find build/ -type f -name *.js.map` 2> /dev/null",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "^10.0.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^15.6.1",
    "@types/node-fetch": "^2.5.10",
    "typescript": "^4.3.2"
  }
}
